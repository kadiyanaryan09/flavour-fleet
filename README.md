Tailwind frontend, Node/Express backend, MySQL — with the exact config/models/controllers/routes/middleware folder split you asked for
10 restaurants, ~150 menu items across 9 categories, realistic INR pricing within your specified ranges
Auth, search/filter/sort, single-restaurant cart rule (matches real Zomato/Swiggy behavior), GST + delivery fee + discount math computed server-side, mock payment, order tracking, restaurant reviews
Admin dashboard with 4 tabs: Restaurants, Menu Items, Orders, Users — all CRUD, backed by real requireAdmin middleware, not just hidden UI
